# Changelog

What changed in _how the work is made_, and which wave paid for it. Newest
first. The campaign record — what exists, what state it is in, what the client
approved — lives in `references/eval-log.md` and
`references/approved-register.md` and is deliberately kept apart from this
file.

## 0.1 — 2026-09-05, the scaffold

- Engagement scaffolded from Armada. Recipe in git, pixels in Drive.
- **Harness shared, judgment not.** `tools/` is the Armada harness at the
  version in `tools/ARMADA_VERSION`. Not one check, anchor, threshold or router
  entry came from another engagement.
- `armada.toml`, `generation.md` and `rubric.md` hold the template's placeholder
  values, marked as such. Rubric 0.1 is uncalibrated and reporting only.
