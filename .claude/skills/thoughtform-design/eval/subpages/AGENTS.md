# Working in this ship

`CLAUDE.md` is the canonical copy of this file's contents for this ship and
is kept in step by hand; read it. The short form:

- Nothing here is drawn. Candidates are Playwright stills from
  `scripts/capture-subpages.mjs` in the site repo.
- Subjects are themes, types are pages, lanes are directions, draws are
  sections, settings are viewports.
- `qa.py`'s preamble calls image 2 an identity photograph; the rubric's
  grading rules override it. Image 2 is a register of other sites.
- The filename grammar takes letters before the first dash; a section rides
  in the draw.
- Run `python tools/doctor.py` after every edit to the three prose files.
- A wave without `ledger.py tick --handback` never happened.
- Never print a credential value. Report a key by name and its length.
