---
description: Scroll metrics, NavigationCockpitV2 hooks, and home-v2 handoff compositing
globs:
  - lib/hooks/useScroll*
  - components/hud/NavigationCockpitV2/hooks/**
  - components/landing/home-v2/handoff-lab/**
  - components/landing/home-v2/home-v2.css
  - components/landing/home-v2/hooks/useDepthScroll.ts
alwaysApply: false
---

# Rule: Scroll animations & cockpit hooks

Shared scroll progress, cockpit integration, and home-v2 corridor-exit compositing. Changes affect Lenis, GSAP, HUD behavior, and the corridor→Services seam.

**Read first:** `sentinel/decisions/002-scroll-animation-architecture.md`, `sentinel/decisions/005-scroll-captured-content-reveal.md`, `sentinel/decisions/018-home-v2-depth-corridor.md`, `sentinel/decisions/021-corridor-exit-zoom-dissipate.md`.

**Process:** `sentinel/MAINTENANCE.md` - document contract changes with an ADR or decision addendum.

**Home-v2 corridor-exit invariant (ADR-021):** the production seam after the corridor epilogue is a **zoom-dissipate** — the docked R3F canvas plays a fly-into-sphere arc + particle scatter, and the destination section's own dark surface re-shields within the first viewport. Keep the dock channel (`docked` / `dockProgress`) writes scoped to one hook; do not write `epilogueProgress` / `paintProgress` from the exit hook. The retired cover-plane sweep recipe (lower opaque plane covers a held canvas, first-read copy lives inside the cover) is preserved in `components/landing/home-v2/handoff-lab/` + `/test/handoff-a|b|c` and documented in ADR-021 — reuse that recipe verbatim if a later section needs an Active Theory / Hashgraph-class cover sweep; do not rebuild it.

**Hero → Thoughtform seam invariant (ADR-022 v8):** the hero → corridor seam is a **ToyFight curtain reveal** — the **hero is the moving layer** (`.hero`, `position: relative; z-index: 4`) that scrolls straight up and off the first viewport, uncovering the corridor's parked `ThoughtformCompassGate` frame which is held **frozen** behind it (`.home-corridor-host` z:3). NOT the inverse (v7's mistake: a held hero with the corridor rising to cover it — "the second section scrolls over the hero"), and NEVER a proxy/copy of the second section (v6's mistake). The freeze: `useLandingScroll` writes `--corridor-pin` (RAW px = `max(0, defTop)`) + toggles `html[data-corridor-entry]` over the first viewport, and `home-v2.css` applies `transform: translateY(calc(var(--corridor-pin) * -1px))` to `.home-v2-stage__sticky` ONLY under that flag, cancelling the cell's natural rise so the parked frame sits still at viewport top while the hero lifts off it. CRITICAL: only the sticky CELL is transformed — never `.home-v2-stage` (its rect drives `useDepthScroll`; transforming it desyncs every corridor channel) — and the transform MUST be gated to the entry band so it is `none` during the flythrough and the ADR-021 docked exit (a transformed ancestor would capture the `position: fixed` canvas and bring back the hard seam line). Hero video stays opaque, never scaled/faded (ADR-008 Rule 3, gateway shield). ADR-022 history documents every superseded approach (v6 proxy sweep, v7 held-hero parallax) and its artifact.
