/**
 * The route's view of the shared tab derivation (ADR-096) — see
 * `proofOrder.ts` beside it for why these shims exist.
 *
 * `trinny-proof-tabs.test.ts` is written against these names.
 */
export {
  filmTab,
  proofTabLabel,
  proofTabs,
} from "@/components/landing/home-v2/services/proof-stack/proofTabs";
