# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
.next
# Alternate build dirs for verification builds (NEXT_DIST_DIR override) so they
# never clobber the `.next` a running dev server serves from.
.next-verify
.next-build
out
dist
build

# Testing
coverage
playwright-report/
test-results/
# Local visual-test baselines: platform-suffixed (-win32) PNGs that would pin
# CI to this machine's rendering, and the theme/rail work keeps staling them.
tests/visual/*-snapshots/

# Misc
.DS_Store
*.pem
*.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.cursor/debug.log
.cursor/debug-*.log
/dev-server.log

# Local agent scratch (logs, audits, draft commit/PR messages, verify scripts)
.cursor/*.log
.cursor/commit-msg-*.txt
.cursor/pr-body.md
.cursor/scan-result-*
.cursor/thoughtform-audit-*
.cursor/home-v2-response.html
.cursor/walls-*-head.txt
.cursor/walls-*-mid.txt
.cursor/verify-next.py
.cursor/plan-extract.txt
.cursor/extract-plan.mjs
# Look-dev capture scratch. Evidence that an ADR CITES is committed under
# docs/design/**; these are the intermediate shoots and their throwaway scripts.
.cursor/isl-*/
.cursor/pda-shots/
.cursor/substrate-r3-shots/
.cursor/arc-portfolio-shots/
.cursor/morph-shots/

# Local screenshot captures (root-only, keeps tracked design/inspiration/**.png)
/*.png
# Root-level capture-script output (capture-adr071-morph.mjs,
# generate-era-stage-mockups.mjs — 46 MB of regenerable look-dev stills;
# the root /*.png rule above does not recurse into it)
/shots/

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# SAM model checkpoints (too large for git)
*.pth
services/sam-segmenter/checkpoints/

# Playwright MCP screenshots and logs
.playwright-mcp/

# Cursor plans (local agent artifacts)
.cursor/plans/

# Claude worktrees and build artifacts
.claude/worktrees/

# Codex CLI local config (agent/tooling scratch, mirrors .cursor / .claude)
.codex/

# Editor-local settings
.vscode/

# Storybook
*storybook.log
storybook-static

.vercel

# Gateway Motion prep (models + analysis masters are regenerable)
scripts/gateway-prep/models/
scripts/gateway-prep/out/

# Local asset staging (raw photography/masters — never deploy via public/)
assets-staging/
public/images/navigator/
.env*.local

# HUD-instruments look-dev stills (96 PNGs / ~8 MB, fully regenerable via
# `node scripts/capture-hud-instruments.mjs`)
docs/design/hud-instruments/
# The HUD panel lab's matrix — ~100 regenerable stills plus its report.
docs/design/hud-panel-lab/*.png
docs/design/hud-panel-lab/report.json

# Voidwalker-avatar preview cache — regenerable via
# `node scripts/sync-voidwalker-avatar-preview.mjs`. Read-only mirror of
# the offline skill's waves folder for the /test/voidwalker-avatar-preview
# route; never checked in (per-machine, 20+ MB per wave).
public/_previews/
