# Intelligence layer — ADR-014 legacy

`OrbitField`, `SplitRing`, `OrbitalCluster`, and `CelestialLinework` implemented the front-on coplanar-ring triad (ADR-014). Superseded by ADR-016 (`TriadScene` + `CelestialBody` + `CometStream`). Kept for `git log --follow` and rollback only — not imported by the active stack.
