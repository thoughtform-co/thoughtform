# Thoughtform.co - Project Context

## Sentinel — patterns and decisions

Read [sentinel/BEST-PRACTICES.md](sentinel/BEST-PRACTICES.md) and the **ADR** relevant to your task before non-trivial changes. Full index: [`sentinel/decisions/`](sentinel/decisions/) (ADR-001 … ADR-051). Load-bearing entries for the current surface:

- **Home-v2 depth corridor:** [ADR-018](sentinel/decisions/018-home-v2-depth-corridor.md) — the dominant landing surface (scroll-driven WebGL corridor)
- **Arc cases (Build park):** [ADR-033](sentinel/decisions/033-arc-cases-orbit.md) → [ADR-042](sentinel/decisions/042-arc-cases-cue.md) — cases orbit, terrace, terminal, card, sigil, cue
- **Services card ring (the conversion beat):** [ADR-029](sentinel/decisions/029-services-card-ring.md) (the ring; the card is ONE object) → [ADR-050](sentinel/decisions/050-services-card-face.md) (tight face + the in-canvas drawer, live behind `SERVICES_CARD_DRAWER`) → ⚠ **[ADR-086](sentinel/decisions/086-services-card-carries-the-work.md) (2026-08-30) IS THE LIVE FACE: `card`, NOT `tight`** — the centre of a services card carries THE WORK, not the practitioner (~40 cards on the owner's reference board, not one carries the person). One cloud, FOUR edge rules: keynote a RADIANT · workshop a ROUTE · embedded a MESH · guided-build a SURVEY whose unlinked nodes are the person-led work. Title pinned to display on a datum measured off the expand chit (the chit never moves — the type does); the `poster` band is SOLVED against the worst-case type, and the figure is HEIGHT-bound so ~328 is its ceiling. ⚠ **THREE THINGS KEYED OFF THE PHOTOGRAPH AND NONE OF THEM ERRORS WITHOUT ONE** — the fetch (334 kB of portraits the landing paid for and never painted), the hologram VEIL (full over y 230–640, i.e. straight through the figure) and the SCRIMS (wrong in dark, a no-op in light); `faceUsesPhoto` is the one predicate all three read, and the veil is silenced on its MATERIAL because `DECK_INTRA_ORDERS` renumbers positionally. `tight`/`full` byte-identical; ⚠ MOBILE still carries the photographs (ADR-083's own surface). Five files move in lockstep — see [`.claude/rules/services-ring.md`](.claude/rules/services-ring.md)
- **Services exit + about:** [ADR-047](sentinel/decisions/047-about-deck-flip-stage.md) (the deck-flip stage — cards stack, flip to portrait, ambient survives past #about and dies at `#voidwalker`), [ADR-045](sentinel/decisions/045-about-emerge-rail-parity.md) (the mobile/fallback about surface; ADR-046's dock is superseded)
- **Proof / client cases (the evidence beat):** [ADR-056](sentinel/decisions/056-services-proof-casefile.md) — the Loop casefile is an interactive viewport (client tabs + terminal directory + swapping evidence panel) at the TOP of `#services`, over the parked brandmark; the card ring waits behind it via a runway split that leaves every ring constant byte-identical. ⚠ **there is NO `#proof` station** — ADR-054's station, `lib/v7-parse/proofStation.ts` and `ProofRevealController` are deleted, and `#practice` inherited the ambient-cover role — which **`#voidwalker` holds since [ADR-074](sentinel/decisions/074-voidwalker-through-line.md)**: the career THROUGH-LINE after the bio (nine beats on a gold spine, six with authored wireframe plates in the casefile's forked grammar, one scroll writer, the record zero-import in `lib/voidwalker/`; rules in [`.claude/rules/voidwalker.md`](.claude/rules/voidwalker.md)). ⚠ **[ADR-082](sentinel/decisions/082-voidwalker-character-stage.md) (2026-08-26) SUPERSEDES ADR-074/ADR-081 ON COMPOSITION** — the station's INTERIOR is a video-game character-selection surface (a rotating 3D era-of-Vince, era rail below, HUD chrome) behind `VOIDWALKER_CHARACTER_STAGE`. The station shell, the manifest row and the ADR-030 §6 cover-lockstep are unchanged; the flag toggles ONLY the interior between the character stage (`.ch*`, `voidwalker/character/`), the ADR-081 travel and the ADR-074 timeline. Six curated eras from the record (`characterEras.ts`) map to a Meshy multi-image-to-3d pipeline in the offline `voidwalker-avatar` skill; ≤ 4 MB per GLB, `probe-voidwalker-models.mjs` guards. Rules in [`.claude/rules/voidwalker.md`](.claude/rules/voidwalker.md). [ADR-054](sentinel/decisions/054-proof-station-client-cases.md) survives only for the content model (`lib/cases/`, types-only + registry, the `lib/arcs` mirror) and the confidentiality envelope. Rules in [`.claude/rules/proof.md`](.claude/rules/proof.md); look-dev at `/test/field-log-lab`. "Case" ≠ "arc page" ≠ `arc-cases/` (LANGUAGE.md)
- **The casefile's right panel is ONE instrument:** [ADR-064](sentinel/decisions/064-casefile-console-frame.md) — all four evidence plates render inside the shared `ConsoleFrame` (`casefile/console/**`), so the panel changes what it displays rather than being four boxes in a slot. ⚠ The frame is a BEZEL THE CONTENT BLEEDS INTO, never a letterbox; below 980px the console UNWRAPS rather than hiding. → [ADR-066](sentinel/decisions/066-casefile-one-rail-one-foot.md) — **one rail, and a row that carries its own rule**: every plate switches on the shared `ConsoleRail` and **NO ORDINAL survives anywhere on the surface**; `--font-sans`, which is declared nowhere, was the "font feels different" bug; a row may carry SHEETS (the Studio row is ads · the line · the red line). ⚠ Its foot law is at the limit case — **NO plate prints a foot since ADR-068 U2** (owner 2026-08-08; the smoke asserts the absence per row). ⚠ **The filter line is AUTHORED vs CAPTURED** (ADR-064 U2, owner) — the tool captures take the services duotone, the stills and films never do, and the smoke asserts BOTH halves
- **The glyphed index + the tool dossier + authored wireframes:** [ADR-068](sentinel/decisions/068-casefile-glyphed-index-and-tool-dossier.md) — the register is a NON-INTERACTIVE glyph+claim+sentence index on all four rows (sentence sr-only below 1070h; the rungs must TILE); the tools plate is bay → capability blocks 2×2 (U2: the blocks render `ProjectCase.capabilities` — one canonical array with the Arc card; the Q&A `detail` field is deleted; U3: blocks seat once on ROW arrival, never on a station switch; the route diagram is deleted, its data held) with SHORT rail handles (diamond back at data-n=4); the Software register speaks for the PROGRAM (left = program, right = tool); wireframes are AUTHORED evidence and **ALL FOUR tools are drawn** (U3 — no img, no duotone anywhere; per-tool label sets smoke-pinned; the capture branch dormant for a fifth tool; the RUN plate and transport chevrons deleted — the simplified watch bar is the one affordance; U4: the bay is UNCAPPED — content fills the housing per the reference boards, the panel's only air is `--tf-gap`; **U5+U6: three drawings redrawn LETTERED around the owner's key features — GREEN flow / ONE gold CTA per drawing** (mímir = three lettered panels INPUT | BRIEFING | AD, green source titles + GENERATE BRIEFINGS; vesper = PROMPT card + image glyph + ENHANCE PROMPT/GENERATE composer CENTRED as one in-flow group in the frame (biased high) with the lettered placeholder `Loop Switch, golden hour` and the card-foot `NANO BANANA` tag in the gold TEXT role (`--w-gold-ink` — raw gold fails the light walk), the composer row fully square (vesper's CTA dropped the chamfer; the bar's width derives from the row's own triplet), its DRAW meter deleted; **U7: the bay's four gold corner BRACKETS are deleted** (the box is already framed, and gold buys one thing per drawing — ADR-065's bracket grammar stands, this box just stopped qualifying), the composition grew (⚠ the tile's `cqh` term is the SIZE and its `cqw` cap only guards W/H < 1.12, so the growth is paid out of `.fl-wire__main`'s bias padding — one budget, and 1280×720 is down to 2.3px of slack), and **ENHANCE PROMPT lost its plate** — wand + label only, the light walk unmoved because `bedOf()` only counts α ≥ .85 and the wash never was the bed (`--w-green-soft` now dormant); babylon = the PROMINENT chain TRANSCRIBE→TRANSLATE→DUB→APPROVE→UPLOAD welded end-to-end over the REAL EN→JA transcript rows, every cell smoke-pinned and chosen digit-free; heimdall untouched on the gold-only grammar), label budget 15, new `--w-green*`/`--w-cta*` tokens in BOTH theme files, ⚠ the CTA's text on the INNER lbl span and the flow connectors as 1px DIVS never svg lines; the CTA PUNCHES THROUGH the halftone veil — relocated to `.fl-wire__in::after` for the three redrawn tools, CTA at z 1, the drawings' one sanctioned z-index, and every ancestor between a CTA and `.fl-wire__in` stays transform-free); ⚠ glyph edits re-run the contact sheet; ⚠ the console unwrap gate pairs width WITH prefers-reduced-motion — and pda.css's map-fallback gate carries the SAME pair
- **The casefile's type ladder + the proof claims:** [ADR-067](sentinel/decisions/067-casefile-type-and-clutter.md) — **two families by ROLE** (PT Mono = chrome, PP Neue Montreal = prose); ⚠ `--font-mono` is IBM Plex Mono, NOT `--fl-mono`, so anything that inherits rather than declaring gets a third face. The proof register is **four CLAIMS** (`CaseBlock` is `{ title, desc }`, title ≤27 measured) — the display figure went because its sixteen values carried nine grammars. Stations are plates underlined by the lit spine at their BOTTOM edge (U1, owner 2026-08-08 — the weld `::after` is deleted with the underline's return), and **ONLY THE LEADING PLATE IS NOTCHED** (U2, owner 2026-08-12): the console's TL chamfer subsumes the first station's cut by ≥8px at every clamp rung, so that notch paints nothing and the rule's only visible output was a floating diagonal on every OTHER tab — `:first-of-type` now, on every rail, with the seam's `top: var(--stn-ch)` shoulder declared rather than inherited from the clip. ⚠ The "two diagonal lines" were the ORBIT RING cropping through the console's top edge (`ry < 525`, `rx ≥ 420`), never the chamfers — a `clip-path` cuts a border, it never strokes one. **1920×1080 is a reference viewport** and is the worst case for `.fl-brief`. ⚠ **[ADR-088](sentinel/decisions/088-casefile-left-column-ladder-and-rhythm.md) (2026-09-02) RE-READS U3's RANK AND DELETES `--lc`** — the left column's four mono/sans roles were one token across TWO FACES whose `svh` term flipped their ranking with viewport height (the sans sentence outranked the mono claim it explains at 1920×1080). Two ladders now, split by FACE at one ratio: the register claim and the directory row are PEERS at `--fl-chrome-lg`, the meta at `--fl-chrome-md`, the sentence at `--fl-copy / --fl-ratio`. Its second half is RHYTHM — the record column is one grid (`.fl-left`, housing, deliberately unmarked) spanning to tick 11, so the directory's last row is SEATED on the tick and the surplus splits 1:2 between the two seams rather than pooling under the rows (137px at the owner's viewport before). Shrinking `--fl-proof-h` to widen the seams further was built and REJECTED (a whitespace seam is not comparable to a hairline-ruled track's pitch). Rules in [`.claude/rules/proof.md`](.claude/rules/proof.md)
- **The casefile is one machined housing:** ⚠ **[ADR-089](sentinel/decisions/089-casefile-is-one-housing.md) (2026-09-02) IS LIVE ON PROOF AND RETIRES ADR-065 U2** — the HUD panel lab's `02 · Housing` promoted: one slab across the instrument band (`.fl-hz`, the services plate's clipped gold lip over glass at 0.42), the header FUSED to its top edge, dawn seams inside. ⚠ **U1 (2026-09-03, owner) STRIPS THE HOUSING'S OWN CHROME AND FINISHES THE CONSOLE'S DEMOTION** — the FOOT BAR is deleted (a machined slab already ends somewhere: its bottom lip is drawn at the rail's last tick, so a 26px band closing it 26px higher was closing a box that was closed; `logCode` joins `stamp`/`state` in the record, lettered nowhere), the head's `state` comes out and the head becomes a RULE (the band above the record is the client's NAME alone, and the line is what the name is underlined into), `+ Archive` is deleted with the tabs-as-clients IA it argued for (the owner's call is **a panel per client**; ADR-087's machinery is untouched and inert at one case), and the RIGHT PANEL loses four leftovers of the box ADR-089 deleted — the top glow (575px of wash modelling an edge that is not there), the console's own scanline (the slab already rules the band), the rail's gold underline (the NINTH structure line, which the lab's four-selector ledger could not see) and the lit station's opaque `--con-void` base (the one opaque object in a glass housing). ⚠ **Taking a console's ground and its border does not take what those two implied** — that is the pattern, and all four rules are `.fl-case`-scoped so the arcs are byte-identical. The CONSOLE becomes a CELL — no chamfer, no hairline, no ground — because the children of a chamfered box are square, and the rail's leading station loses its notch with it (that cut existed only because the console's chamfer fell on it, ADR-065 U3). The defect it answers is measurable: eight gold structure lines at .12–.24 against a frame running 2px of dawn at .55. ⚠ **U3 (2026-09-04, owner: _"instead of tabs can't we try like boxes like here?"_) MAKES A STATION A BOX AND ITS SELECTION AN EDGE** — the reference states one law twice and neither half is a fill (three DETACHED bordered boxes of which none is filled, a year selector marking the open cell with an OUTLINE), so **selection is an outline plus one small filled mark** and the rail already owns the mark. The plate gives up its ground entirely (U2's `.08` whisper was the wash apologised for rather than replaced), the open box takes `--gold-line`, and the key-to-key seam is DELETED because a gap between two boxes is already the divider. ⚠ **THE SPINE GOES WITH IT AND ADR-063's LAW SURVIVES** — the first cut kept it and shipped a **doubled gold rule** (the box's own base plus a second gold line 4px under it, a full pitch wide so it ran 3px proud), the third time on this surface, **every gate green and the still not**; the law is about COUNT, and there is still one lit thing travelling key to key — it moved from the track onto the object. ⚠ **MARGIN, NEVER `gap`** (a gap makes each station `(100% − (n−1)g)/n` and the marker drifts a gap per station); ⚠ **the leading station's 9px chamfer clearance was DEAD and was pushing its content 4.5px off centre**, invisible on a full-bleed plate; ⚠ **the box costs 8px of label headroom** and 1280×720 is down to 8px on `BRIEFING AGENT`; ⚠ **the lab's `v6` diamond flipped twice in two days** because the rule only ever had an opinion about what it was sitting ON — a lab rule reading a production surface is a rule with an off-site dependency. ⚠ **THE ERA STAGE IS NOT TAKEN** — the owner held it, and the committed `>700px` paint sweep on `#voidwalker` (ADR-082 U21) fails a band-width housing three ways at once. ⚠ Three lab defects did not survive production, all found by LOOKING: a cell head clipped by its parent's `overflow: hidden`, a one-child `1fr auto 1fr` header landing a third of the way across, and `state` printed twice. Rules in [`.claude/rules/proof.md`](.claude/rules/proof.md)
- **The corner law:** [ADR-065](sentinel/decisions/065-corner-law.md) — chamfer = a machined housing · notch (one corner) = oriented or connected · bracket = framed but not a device. ⚠ **U2's enumerated TL+BR console is RETIRED by ADR-089** and **U5's premise moved with it** (the console is no longer the housing its seated set answers to — the capability plates' notch is an open ruling). One grammar per object, **the diagonal is TR + BL** (TL+BR only as the mirrored back of a flipped object — the casefile console was the one exception and is corrected), depth ladder seed `16px` / plate `26px` / chrome `0`, and **the children of a chamfered box are square** — which is what stops the surface reading flat. ⚠ **U4 (2026-08-13): in the notched-SET exception the operative clause is "ON THE LAWFUL DIAGONAL", not the count** — reading 01's twenty work cartridges satisfied every other clause with a uniform TOP-LEFT notch, and a set that is internally consistent is exactly the case the eye and a first-clause read both pass; they carry the TR+BL pair now, so the exception is not invoked there at all. A single notch also MEANS oriented-or-connected, and twenty identical cards in a grid are neither. ⚠ **U5 (2026-08-14, owner): a SEATED set takes ITS HOUSING's diagonal** — the tools plate's four capability blocks notch **BOTTOM-RIGHT**, reversing U1's own "the dossier's plates take BL" for that set alone. U1 flipped the mockup's unlawful TL and reached for the SITEWIDE lower end without asking which diagonal a nested set answers to; these plates sit one level inside the console, which is the law's one enumerated TL+BR object (U2), so its lower end is BR. Sitewide TR+BL is untouched and the clause can only reach a set inside that one housing. ⚠ Same guard shape as U4 — the smoke asserted `squareBL === false`, which verifies the notch IS BL rather than that it is on the right corner; corners are pinned from BOTH ENDS now. Summary in [DESIGN.md](DESIGN.md#the-corner-law-adr-065)
- **The Intelligence Map (the casefile's lead row):** the live panel is the **PDA console** (`casefile/map/pda/**`) — a held instrument with three readings (01 THE WORK · 02 THE CONFIGURATION · 03 THE SUBSTRATE). ⚠ **[ADR-069 U1](sentinel/decisions/069-pda-selection-morph-and-answered-configuration.md) (2026-08-13, owner) IS THE LATEST PASS: THE CARD IS ONE DRAWING AT TWO SIZES, AND IT WAS TWO.** ADR-069's claim is that the selected work is a PERSISTENT OBJECT that flies between readings 01 and 02 — but U2→U13 redrew reading 02 five times while reading 01 kept v18's cartridge, so the object arrived having changed its corners (TL notch → the canonical **TR+BL pair**, ADR-065 U4), its state glyph (a floating 22-unit circle → R4's squared `StateMark`, **moved up into the header row** — that band is what pinned the title at 68 % down), its colour (**green → gold**; green is the human and nothing else, so the flight was recolouring its own cargo) and its title's height. ⚠ **EVERY GUARD WAS GREEN**: `pda-flight` measures the two RECTS, which is a silhouette, and each interior was measured only against itself — the defect lived in the RELATIONSHIP, where no per-object test looks. `CARD` (pdaGlyphs) is `SEAT`'s values ÷ `CORE_K` rung for rung, `StateMark`/`LaneMeter` are shared, and `tests/lib/pda-card.test.ts` holds the pair **including a guard that fails on a rung added to one card and not the other**. ⚠ **THE TITLE'S BASELINE IS THE ONE DELIBERATE DIVERGENCE** — the seat fills the space under its title with THE BAR and the grid letters no bar (owner: styling, not content), so seat parity would pool 59 % of the small card into one hole; `configLayout`'s **split the slack, don't pool it** applies, lifting the title 68 % → **53 %** with the header and foot at parity. ⚠ **`autonomy` CAME OFF THE CARD** with the lane meter (owner) — reading 02 letters it on the OWNER PLATE, and the foot is one left-anchored RUN now, not a pinned pair. Also fixed: `Cartridge`'s bar was `fontSize="10"` unscaled (the labs' 5.4px minPx) and `cartTitleChars` was missing `k` (42 characters allowed where 21 fit). — ⚠ **[ADR-070 U15](sentinel/decisions/070-configuration-is-a-switchboard.md) (2026-08-12) IS THE PASS THAT TOUCHES ALL THREE READINGS.** ⚠ **ITS READING 03 IS SUPERSEDED BY [U23](sentinel/decisions/070-configuration-is-a-switchboard.md) (2026-08-16, owner): ONE PLATE DIVIDED INTO FIVE REGIONS OF MATERIAL — area is the Skill count, NO GUTTERS, one outer cut; each region letters its name, its count and ONE PARAGRAPH (`CaseMapShape.meaning`, new, ≤96 chars and the only field the projection does not uppercase) over its own physics field, with a graduation of one tick per encoded Skill at its base. ⚠ **U24 (2026-08-17) PUT THE 47 NAMED SKILL PLATES BACK** — two columns per region, seated at its floor, and the tick graduation is deleted with their return (U23 kept the count but lost the DENSITY, which is why the reading felt unlike 01 and 02); `gloss`/`evalMethod` still letter nowhere; the division is a GROUT (a 1-unit rule paints 0.65 device px here) and a grout is not a gutter; density is PER UNIT AREA; and ⚠ `BOX_H0` is 696 because a width-bound elastic crop's ceiling is the NARROWEST field's aspect (the lab's 0.8176 is height-bound at 1440×800). ⚠ **[U34](sentinel/decisions/070-configuration-is-a-switchboard.md) (2026-08-23, owner) IS THE LATEST PASS ON THE CARRIER: THE DODECAGON IS THE HOUSING AND THE DIVISION INSIDE IT IS CONCENTRIC.** Every ring was 12-sided while every label rode a CIRCLE, and a dodecagon's radius dips to `κ·R` (13.1 units at `R_OUT`) at each of its twelve edge midpoints — so **19 of 47 labels' ink crossed their own cell's outer wall** (`Feedback` −5.0u, `Localization`/`Supplier QA` −3.3u) while every guard reported 7–12 units of air on BOTH sides, because `substrate-lab-fit` measured `cell.r0`/`cell.r1`, the NOMINAL radii, which are the wall only at the twelve vertices. ADR-069 U1's finding one level down: **the guard measured a model of the drawing rather than the drawing** (U32's ink-centring correction is real and is 3.5u; this was up to 13.1, same direction, same labels). ADR-065's law one level up settles it — the housing carries the machined geometry and what is seated inside does not repeat it: the silhouette, the hub, the band's ring and the outermost cells' outer edge stay 12-sided, the **four internal course seams per part become concentric**. ⚠ **THE SEAMS ARE SAMPLED POLYLINES AND MAY NEVER BECOME `A` COMMANDS** — ADR-071's morph interpolates CSS `d` against `cell.d` and needs one command structure (`SEAM_STEP` 3° = 0.10u of sagitta at r 300). ⚠ **ONE RULE, AND THE ASYMMETRY IS THE FINDING**: a circular wall is exact, a polygonal INNER wall is worst at its circumradius, a polygonal OUTER wall is worst at its **apothem** — `CarrierCell.outerWall` carries the third case and `carrierCellArcRadius` centres on `[r0, outerWall]`. ⚠ The apothem is all but exact here (every rim cell comes within **0.081u**), and **the sweep's width is not a proof of that** — the first guard used `≥ 30°` and failed on `pattern`'s four 25.9° rim cells that clear it anyway; the property is sampled, and the miss grows to 5.9u at `w` 10°. `carrierPolygonShare` = `√(P/C)` (0.9758–0.9779) rescales the partition and `sectorTerm` integrates each wall with its own constant. ⚠ **A SEAM IS SHARED, SO IT CANNOT BE SOLVED PER CELL** — 0.42 % spread for 1.06u of seam wobble was measured and REJECTED; the area guard pins the STRUCTURE instead (concentric-bounded cells to 1 %, the two housing-bounded courses to 2.5 %, blanket 4 % → 4.5 % against a measured 3.97 %). `LABEL_PAD` 12 → **10** and `MIN_CELL_DEPTH` 26 → **23** (the depth ends at the apothem now), and **all five ladders come out byte-identical to what shipped** — the plate does not re-cut, the labels move inside it. ⚠ **AND PATTERN/STAKEHOLDER WERE NEVER MISSING A BACKGROUND — THE BAND NEVER HAD ONE**: they are the two regions the open stream does not draw on, `TapWash` filled the other three at gold α.14, and the band was the only region on the plate with no material of its own. One recess wash on all five; ⚠ **0.10, because the first cut at 0.03 was a fix that changed nothing** — it measured `rgb(11,10,9)` against the plate ground's `rgb(11,9,5)`, inside a value of the thing it had to be distinguishable from, against a **6× luminance ratio** on the tapped three. FLAT, not grained (the hub is a CORE and answers with a grain; this is a machined RECESS, a cut face), and it darkens in light because ADR-058 swaps `--dawn-rgb` — a step away from the ground in both themes. ⚠ **THE LIVE GUARD IS THE DURABLE HALF**: the smoke probes the ink's two extremes along the up-vector and asserts `isPointInFill` on the cell's own path — two labels not printing through EACH OTHER and a label not printing through its own CELL EDGE are different questions and the second had none. ⚠ **The ink block is ASYMMETRIC about the baseline** (ascender 0.769em above, descender 0.231em below) and swapping them reports every tight cell as a spill. Calibrated, not merely green: 0 spills at +0/+3/+5, **1 at +6**, 27 at +12. ⚠ **Left open: the tap's signal is weak in LIGHT** (L 0.561 lit against 0.578 recessed — carried by hue, not value), pre-existing and ADR-063 U2's own finding; the honest fix is the gold ramp's INK rung, not a bigger alpha. — ⚠ **[ADR-071](sentinel/decisions/071-skill-flies-to-substrate.md) (2026-08-19, owner) SPLITS THE FLIGHT: THE SKILL IS WHAT LANDS ON THE CARRIER, NOT THE WORK — AND SINCE U1 (same day, owner: "we just see a frame floating, and then it fades away") ITS ARRIVAL IS A SHAPE MORPH, NOT A DOCK.** U33 seated the work card in the carrier's hub as a third home; ADR-071 replaces it with the SKILL CHIP — a second persistent object on the 2↔3 axis while the work card stays 1↔2. Reading 02's WHAT RUNS IT node hosts a `SkillChip` (square-cornered plate, engine's physics field, gold ink); on 2↔3 the PLATE'S PATH ITSELF INTERPOLATES (CSS `d`) between the chip's rectangle and the destination cell's OWN ring — `carrierChipMorphIn/Out` emit `{from,to}` with ONE command structure (a mismatch does not error, it snaps discrete; `pda-flight` pins the counts for every configured stream, and `morph.to === cell.d` byte-equal), with `CarrierCell` storing its drawn `outer`+`inner` polylines as the one source. The NAME flies its own `pdaFlight` on its own rects (`skillChipNameRect`/`carrierSkillNameRect` — left-anchored in the plate, centre-anchored on the arc) at `dk = CARRIER_LABEL_FS / CHIP_FS` (13/14), landing text-continuous with the cell's arc label. ⚠ **THE MORPH HAS ITS OWN CLOCK — `PDA_MORPH_MS` 620 on an ease-in-out — because the dock's curve at 420ms put the path 87 % home in the first 100ms (measured), i.e. the shape change was spent before the eye arrived; `PDA_FLIGHT_GUARD_MS` rose to 650 to cover the longest gesture.** The seated cell LIGHTS ON TOUCHDOWN (`.fl-pda-seat-arm` holds resting paint for the morph's duration), the closing fade removes a pixel-identical layer (nothing floats, nothing pops), and on 3→2 the real chip stays hidden under `.fl-pda-chip-reveal` until the wedge-become-rectangle lands on it. Verified live by `scripts/capture-adr071-morph.mjs` — samples the COMPUTED `d` mid-flight (continuous both directions) and captures frames under a 12× slowed animation clock, since Playwright's roundtrips cost 400–900ms and unslowed "mid-flight" frames are post-fade fiction. ⚠ **THE JOIN IS `cfg.skillId: string`**, required for every configured stream, guarded by `cases-registry.test.ts` (`engine.toLowerCase() ∈ shapes`). Twelve of 24 mappings are FLAGGED for owner review in `loop-earplugs.ts` — the rendered SKILL name changes from `s[0]` to the roster's `short` (e.g. W-017 "Brand voice" → "Paid Social", W-046 "Status digest" → "PRG Status"). ⚠ **`CARRIER_SEAT_RECT`, `CARRIER_HUB_K`, `SeatedWork` and `Aperture.selected` ARE DELETED**; the hub returns to the brief (`BriefReadout`) whether or not a stream is open. `pda-flight`'s third-home suite becomes a skill-flight suite; `pda-card`'s hub-seat suite is replaced by a `CARRIER_CHIP_K` derivation; `flPdaDock` gains `rotate(var(--dr, 0deg))` (work-card flights pass no `--dr` — byte-identical fallback). — ⚠ **[U33](sentinel/decisions/070-configuration-is-a-switchboard.md) (2026-08-18, owner) IS THE LIVE READING 03: THE COMPOUND CARRIER** (`PdaCarrier.tsx`) — one dial, forty-seven cells each lettered along its own arc, five substrate names in a band, and — pre-ADR-071 — the selected stream seated in the hub as ADR-069's third home. ⚠ **[U36](sentinel/decisions/070-configuration-is-a-switchboard.md) (2026-08-23, owner) MADE THE BAND CLICKABLE — the hub answers with the shape's own sentence.** `CaseMapShape.meaning` is the map's only prose and it lettered NOWHERE for two redraws (U23 put it on the divided plate, U25 in a stratum head, the carrier had no room for a paragraph) — a required, envelope-scanned field the drawing held and never said. Each band segment is a `.fl-pda-hit` group shaped like a cell's; ⚠ the WHOLE SEGMENT is the target, never the word (a `textPath` run is a thin ribbon on a curve), `fill="transparent"` never `none`, and pinning a shape/cell is MUTUALLY EXCLUSIVE at both setters (two pins make `Aperture`'s order a silent tiebreak). `litKey` follows the pinned shape, so clicking a name lights its region for free. ⚠ **THE HUB LETTERS NO COUNT — a guard from the owner's own U28 ruling killed the first cut** (`"the nameplate's count row came back"`): this drawing counts by AREA, and *every future pass that wants to say how much reaches for a digit first*. ⚠ **`SHAPE_PER` IS 27, NOT THE BRIEF'S 30** — the hub's worst edge normal is at 30° where clearance is `apothem − 0.866·halfWidth − 0.5·halfHeight`, so WIDTH costs 1.7× height; at 30 the worst block cleared 17.7u, at 27 it clears 35.2 with the same line counts (guard at 24, the brief's standard). ⚠ **`outline` ON AN SVG GROUP IS DRAWN AROUND ITS BBOX** — the UA focus ring was painting a 151×86 white box over the neighbouring cells (the 47 cells had it too; a wedge 4× the area made it visible); `.fl-pda-hit` clears `outline` in every state and strokes its FIRST path, and it keys on **`:focus` NOT `:focus-visible`** because `matches(':focus-visible')` measured FALSE on the very group the UA was ringing. ⚠ Left open: `evalMethod` still letters nowhere. — ⚠ **[U35](sentinel/decisions/070-configuration-is-a-switchboard.md) (2026-08-23, owner) RETIRED `SUBSTRATE_SECTION`, `PdaSubstrate.tsx` AND `pda-substrate-fit.test.ts`** — the carrier is reading 03 unconditionally now, `PdaConsole`'s 6 flag branches and the guards' 12 are straight lines, and `VIEW_BOX[3]` carries `CARRIER_VIEWBOX` instead of SECTION's crop (which `pda-viewbox` had been working around at its own boundary). ⚠ **`estateBand.tsx` STAYS** — it looks like SECTION's private helper and `VariantManifold` imports it, so deleting the drawing's helper with the drawing would have taken a live lab variant with it, silently: **a file's consumers are not always in the tree you are cleaning**. The lab's `shipped` entry went too (it was the one variant OUTSIDE the `DRAWINGS` record, mounting production with bespoke props); `carrier` is the baseline and `DRAWINGS` is a total `Record<IslVariantId, …>` again, so the compiler guards every id with no carve-out. ⚠ `capture-substrate-lab.mjs`'s default `--v` now names `carrier`, but **the rest of that list is still round one's — every later direction is ungated unless named**. The pre-U34 flag rationale, for the record: the two drawings did not share a flight home (SECTION's was a ghost footprint, the carrier has none), so the flag had to reach into `rectFor` and not just the render — gating only the drawing would have left `rectFor(3, id)` returning `null` with no throw, no failed render and no failed guard. ⚠ **`HUB_K` IS DERIVED — `LABEL_FS / CART_TYPE.title` = 1.1304** — the work's name letters at the rung the 47 Skill names around it letter at, and the resulting `198.96 × 153.74` carries the cartridge's aspect to the last representable digit, so the flight's uniform `dk` needs NO distortion term (where SECTION's simplified footprint needed a 3 % tolerance). An estate band in SECTION's manner was rejected: 62 units of crop height is `meet` 0.634 → 0.586, i.e. every label back under 8px to buy a footprint the hub holds for nothing. ⚠ **A CROP MUST BE ELASTIC ON WHICHEVER AXIS IS SLACK** — U32 made this crop elastic in WIDTH and recorded why the height could stay fixed (the plate's aspect is 1.035 and every field is wider), true at the three lab presets and FALSE on a tall desktop window: **132px of dead panel at 845 × 950**, the owner's own monitor, within 5px of the 265px that forced U15's generalisation on this same reading. Three passes have now shipped a crop that fills the panel at its authoring viewport and letterboxes at the other end, each one green, because `minPx` measures the drawing against ITS OWN CROP. `carrierCrop` grows one axis or the other around the hinge `W_MIN / CROP_H` = 1.033, both terms floored; `CX`/`CY` stay constants and the CROP's offsets slide around them, which is what keeps all 47 cells and both label rings written against constants. ⚠ **`CARD_BOX` IS THE SILHOUETTE'S ONE SOURCE NOW** — `176 × 136` was declared independently in `PdaViews`, `PdaConfiguration` and (this pass) `PdaCarrier`, and three copies is not a wrong value but a value that can go wrong in ONE place while every per-home guard stays green, which is ADR-069 U1's finding exactly. ⚠ **THE SMOKE'S OVERLAP GUARD CANNOT BE ASKED ABOUT ARC-SET TYPE** — the first live capture reported 22 colliding label pairs with nothing touching on screen, because `getBBox` is a valid ink proxy only while type is HORIZONTAL; `readPda` splits by how a label is SET (flat keeps the box test, `textPath` is walked per-glyph via `getStartPositionOfChar` against 3.9 units), and the arc-label COUNT is pinned too, since a `textPath` that stopped resolving would EMPTY the list rather than fail it. ⚠ **THE LAB IS A WINDOW ONTO PRODUCTION, NOT A COPY** — `VariantCarrier.tsx` re-exports `PdaCarrier`, so `substrate-lab-fit`'s 263 assertions walk the SHIPPED module and there is no second drawing to diverge; the dependency runs lab → production and may not reverse (`app/(internal)` is proxy-blocked in production). ⚠ The flag is a COMPARISON LEVER, not a permanent seam — when the owner has read both live, the losing drawing and its guards go. — ⚠ **[U25](sentinel/decisions/070-configuration-is-a-switchboard.md) (2026-08-17, owner) IS BEHIND THAT FLAG SINCE U33; it SUPERSEDED BOTH U23 AND U24 WITH THE `SECTION` DIRECTION** — reading 03 answers the CLICK it arrived from now. U24 kept the roster but threw away the click's context: readings 01 and 02 shared the selected work at their own scales, 03 threw it away. SECTION resumes it. Composition: twenty ghost cartridge FOOTPRINTS across the top (the estate at footprint scale, with the lit-edge grammar on the reader's open stream) over a five-lane GALLERY band, five STRATA below ordered lightest (Stakeholder 5) at top → heaviest (Pattern 14) at the floor, one riser SHAFT descending the left with stubs into every stratum. Each stratum: `name · count | meaning ¶` at the head, plates SEATED AT THE FLOOR in FIVE columns (not U24's two — wider strata afford it), with the pattern's physics field beneath. Selection lights ONE gold PATH — footprint → gallery lane → shaft lane → tapped stratum with a diamond mark. ⚠ **THE PROPORTIONAL CLAIM MOVES FROM AREA TO BODY** (heads are fixed chrome and cannot proportional-shrink, so `bodyPerSkill = (strataH − 5 × headH) / totalSkills` is the shared unit, walked at every field shape). ⚠ **ADR-069's PERSISTENT OBJECT HAS THREE HOMES NOW**, not two: reading 01's grid cartridge, reading 02's core seat card, and this footprint. `PdaConsole.entryFor` factored into a `rectFor` helper that returns any reading's home; `pdaFlight` UNCHANGED. `pda-flight` gained 12 tests covering 1↔3 and 2↔3 round trips at the binding and tall viewports, plus footprint-aspect parity (<5 % against the cartridge). ⚠ **THE 5 × 8 CROSSING STAYS GONE** — the shaft's five lanes are shape lanes, not team lanes; `PdaWork.taps` is the per-stream projection, and `GALLERY_LANES === SECTION_ORDER` so conductors from footprints do not cross. ⚠ **THE CARTRIDGE SILHOUETTE APPEARS ONLY IN THE ESTATE BAND** (footprint scale), because those ARE workstreams; the strata below are square-cornered full-width bars (ADR-065's children-of-chamfered-console rule). Retired lab entries: `34 · Section` promoted (byte-identical in production now), `36 · Control` cannot answer its own question once U24 is off the site; `35 · Manifold` survives as the losing round-nine alternative. It supersedes U16's five pattern cards** (`PdaSubstrate.tsx`) — the reading is EXTRACTION (plates = what is encoded, field = the material they came out of), the label is an AUTHORED `short` ≤14 (clipping gives "Legal Risk Met"), the first encode's ACCENT goes green and its label does not (green ink against `--pda-txt` makes the highlighted plate the dimmest thing in the stack), and ⚠ **THE 5 × 8 CROSSING IS NOT DRAWN ANYWHERE ON THE SITE NOW** by owner ruling — `crossing()` still projects it and its arithmetic is still guarded, but it cannot return inside a card (eight codes need ~196u against a 132u window, and marks without codes need a legend this surface bans). ⚠ Two constants are load-bearing where the obvious value fails: `GLOSS_LINE_BOX` 17 (a 12-unit label's `getBBox` is 15.47 — the em box, not the ink — so a 15 pitch overlaps by 0.47 against a 0.5 gate) and `GLOSS_PER` 16 (130.6u of a 132u window). ⚠ And twice over, a 1-unit rule paints under a device pixel here, so THE SPINE IS THE STACK'S OWN LEFT EDGE and THE FOOT SEPARATOR IS A BAND. U15's own pass — the **PIN GRID** (five patterns × eight departments in one socket; the mockup IS the live record drawn, 30 taps / 5 cut / 10 empty) — binds only for the ELASTIC CROP machinery below. It had replaced the crossing drawing, whose `Module` cards cut `h × 0.34` on BOTH left corners (68 % of the left edge, beside `Plate`'s flat 8 and `Cartridge`'s `14k` — three glyphs, three grammars), and whose `{n} TEAMS` line rendered the banned **`8 TEAMS`** on the public page because a string composed at render time is outside every content scanner. **And ALL THREE crops are elastic now** (`pdaFit.ts`): U12/U14 generalised, because 01 and 03 were carrying the same dead-panel defect while 02's guard was green — 117px horizontal on 01, **265px on 03 at the owner's viewport**. Make the crop's aspect equal the field's and `meet` is unchanged, so growing a crop is FREE (and buys ZERO type). ⚠ `gridRect` takes the LIVE layout, un-defaulted; `pda-viewbox`'s ≤40-unit waste rule is REPLACED (not deleted) by a centring contract + a seven-field fill suite; `pda-substrate-fit` is reading 03's first arithmetic guard. — ⚠ **U11 (2026-08-11) IS THE LIVE DRAWING FOR READING 02: the R4 SUBSTRATE FIELD, built from the owner's `design_handoff_intel_config_r4`. It SUPERSEDES U10's geometry/type/materials, which superseded U2–U9's; all of them still bind on CONTENT and on the flight.** The composition is the handoff's module table verbatim in its own 888×744 stage coordinates (owner `232,20,424×108` · left `4,192,204×218` · right `680,192,204×218` · base `244,532,400×128`, card centred on `(444,300)`): opaque modules on a faint PCB bed, **two opposed 45° cuts — TR+BL since U13** (owner, 2026-08-11: R4 draws TL+BR and the owner put the drawing back on ADR-065's CANONICAL diagonal — the one place the reference is overruled by a standing rule rather than by arithmetic; ⚠ the plate and its housing now lean OPPOSITE ways, since `ConsoleFrame` keeps its U2 TL+BR override, and that mismatch was the whole argument for U11's cut. ⚠ two things ride the diagonal: the 2px top rule STOPS at the cut (`x → x+w−CUT`, three call sites) and the header band needs its own `band()` path — the full `housing` puts a 45° nick at the band's bottom-LEFT, mid-module where no edge exists), a 2px top rule each, **8-wire hatched ribbon lanes** on all five docks, and one role law — **gold is wayfinding, green is the human and nothing else**. ⚠ **THE BOARD IS HEIGHT-ELASTIC (U12) — ONE STATIC CROP CANNOT SERVE THIS PANEL.** The console's field is capped at 850px wide but grows with viewport height, so its aspect runs 1.24 (laptop) → 0.76 (2560×1440); `meet` fits by the SMALLER ratio, so **U4, U10 and U11 each picked one end and letterboxed the other** — U11 cost 270px of dead panel on the owner's own monitor _while every assertion stayed green_, which is a GUARD defect: nothing measured the drawing against the PANEL, only against its own crop. `configLayout(configExt(fieldAspect))` fixes it — the crop's WIDTH never moves, so the fit stays width-bound and `meet` is `field.w / 932` at every height, i.e. **growing the crop's height is FREE** (minPx byte-identical at every shape: 7.76 / 8.74 / 10.94). The height goes to the CABLES (R4's own grammar — a taller board is a longer run) and the CELLS; ⚠ **the card is not in that list** (its box is the flight's destination — it re-centres in the band) and ⚠ **the added cell air is SPLIT, not pooled**, or a taller module is just a module with a hole under it. ⚠ **AND THERE IS NO TAIL SHARE (U14, owner)** — U12 hung the remainder off the bottom as invisible bed (26 units of air above the seat against 135 below at the owner's shape, so the block sat high over a hole); **the margin is DERIVED and SPLIT** — the module block takes what the cables and cells claim and the rest is halved above and below it, centring the board by construction at every height. The BED spans the CROP now (R4's own 744-unit stage mapped onto it), so there is texture above the seat too. ⚠ **`CONFIG_INSET` IS HORIZONTAL-ONLY**: side margins are the fixed frame inset, vertical is derived, and `pda-viewbox` asserts them SEPARATELY — collapsing them into one "uniform inset" test is how the tail returns unnoticed. `CONFIG_EXT_MAX` 620 fills every desktop shape; only a PORTRAIT desktop window reaches it, and there it letterboxes on purpose because a 590-unit bus run is a gap with wires in it. ⚠ **THE FLIGHT USES THE LIVE BOARD** — one `configLayout` object feeds the attribute AND `pdaFlight`. ⚠ The `ResizeObserver` cannot feed back STRUCTURALLY (the SVG is absolutely positioned, so CSS sets its box and a `viewBox` change cannot move it). ⚠ **THE CROP'S WIDTH IS THE REFERENCE'S FRAME, NOT ITS STAGE**: at rest `-22 -6 932 751`, a uniform **26-unit inset** around content `4…884 × 20…719` — cropping to the stage measured better (meet 0.679 vs 0.647) and put the side modules **2.7px off the console wall**, and _a technical drawing whose outermost rule touches the wall has lost its margin, not bled_ (ADR-064's bleed law is about a CAPTURE filling its bay). The aspect is what buys the type: 1.241 against measured field aspects 1.118–1.239 gives meet 0.541 → **0.647** at 1280×720 and 0.833 → **0.912** at 1920×1080, **+20 %/+9 % before a font size moved**. ⚠ **A 1:1 PROTOTYPE CANNOT CARRY ITS TYPE INTO A BOX THAT SCALES** — R4's 8.5 label renders 5.5px here (the size the owner called "utterly illegible" one day earlier) and its 6.5 chrome 4.2px, under the smoke's 4.3 floor; so R4's RANKING is kept and its bottom rungs lift to the floor (title 22 · value 14 · owner 14 · question 13 · key 12.5 · chrome 12, floor **12**), the lost range bought back in **ALPHA, which does not shrink with `meet`**. ⚠ `pda-viewbox`'s waste guard **changed its QUESTION, not its strictness** — reading 02 is WIDTH-bound now, so it asserts the four equal margins inside `[18,34]` while 01/03 keep the height rule. ⚠ **SIX LETTERED THINGS IN R4 ARE DELIBERATELY ABSENT** (side stamps + passive designators = invented ordinals in costume and 3.9px; header metas = the question said twice, and they do not fit at a legible size; ribbon tags = `LANE` collides with the model lane; satellite meters = a gauge beside a client's named Skill implies data this case does not publish; the `DRAW —` prefix = on this record `draw` is WORKLOAD) — **the bed's MARKS stay**, its texture never depended on the letters. **THE LANE LADDER IS THE ONE DELETED THING THAT RETURNED**, re-pointed: U4's meter measured workload and needed NEVER A PRICE, this one is the capability LANE (generic by law, already published, exactly four values), so the gauge IS the record — and four cells with two lit is the scale U10's bare "everyday lane" never had; guarded on the four lanes + `NO LANE`, no `DRAW`, person-led absent, and the record must still span all four rungs. ⚠ **`CORE_K` IS 1.7 AND R4's CORE IS NOT SIMILAR TO THE CARTRIDGE** (300×224 = 1.339 vs 1.294; a uniform `dk` cannot carry a shape that changes proportion), so `CORE_RECT` stays `176×136 × CORE_K` centred on R4's own core centre, and the card's cut stays PROPORTIONAL (`14 × CORE_K`) where satellites take R4's flat 12. ⚠ **AN ALPHA CEILING IS SET AGAINST THE RENDERED DRAWING, NOT THE 1:1 CANVAS** — at meet 0.647 a 1-unit hairline paints 0.65 device px and the browser pays the rest in alpha, so R4's ~.14 bed arrived at ~.09 and vanished (group opacity 0.85). → the U10 record: the crop was `0 48 1000 912`, **LANDSCAPE** — the console field is capped at 850px wide but grows with height, so it is landscape on laptops (1.12–1.24) and portrait on tall screens (0.42–0.81), and U4's portrait crop was costing 155–181px of horizontal letterbox on every laptop; `pda-viewbox`'s aspect assertion INVERTED with it (≥1.05 plus a bound on the tall field's type) because **an aspect contract deleted rather than flipped is one nobody notices breaking**. Nothing letters under **12** (U9's keys at 10 rendered 5.4/8.3px, under the 8.5 chrome floor — "a label nobody can read is not a quiet label, it is an absent one"); ladder question 13 · key 14 · value 15 · owner 16 · bar 14. The seat's dashed line is a **PYLON** — U5's law kept, but the distinction moves from WEIGHT to MATERIAL (authority is structure, data is conductors). ⚠ The card is **`SeatCard` in `PdaConfiguration`, not `Cartridge`** (whose offsets are absolute multiples of `k`, so at k 2 its bar bottomed out 3 units off the floor) — **the SILHOUETTE may not move**, `CORE_RECT` stays `176×136 × k` at k 2 for ADR-069's flight, and ⚠ **its three strings are declared now**: while it was `Cartridge` the guard was walking a drawing with three invisible labels in it, which **any reading that mounts a production glyph inherits**. One ink, keys in Tensor gold `--pda-ink`, hatch/dashed-inset → a divider rule, plus the services cards' **bezel** (⚠ its inner chamfer leg is NOT `leg − inset`; a 45° cut offset by `d` moves its diagonal by `d√2`). `MODEL` letters the verbs not the lane, and `AGENT` may not be a codename. → the U2–U9 record: **reading 02 is the owner's UNIT BOARD** (ADR-070 U2, 2026-08-10 — the composition IS `configuration-unit-mockup.html`, adapted; U1's radial switchboard lasted one day because it decluttered its own skeleton instead of installing the mockup, and **when the owner supplies a mockup, the mockup IS the composition**): ONE lit card centre (the cartridge at `CORE_K` carrying **THE BAR on its own face** — optional `bar` prop, reading 01 byte-identical; the bar block is the `gat` hover, its hit bed a SIBLING of the dock group), the owner above on **ONE DASHED HAIRLINE, never a bundle** (U5 — the seat is AUTHORITY not data, so it is answerable-to rather than feeds-into, and the contrast with the nodes' thick bundles is the reading; the autonomy is the plate's own RIGHT COLUMN, so nothing floats between plate and card), **three question-headed nodes** (⚠ **RE-SLOTTED IN U9, owner 2026-08-11, WITH THE GEOMETRY UNTOUCHED** — WHAT RUNS IT · WHAT IT CAN REACH · **WHERE IT RUNS**, which REPLACES WHAT IT INHERITS; k-labels SKILL/MODEL · **KNOWLEDGE GRAPH/CONNECTORS** · **AGENT/INTERFACE** at fs 10 `--pda-txt2`. Two slots were WRONG and this corrects them rather than restyling: a graph is QUERIED through a connector (reached, never inherited) and an interface is where a PERSON MEETS the work (never something it reaches) — so the graph LEADS its node and the connector follows. The headers are BACK by the owner's own hand; material grounds: green hatch BELOW the value's line box on SKILL, the dashed BLUE inset now on KNOWLEDGE GRAPH — `--pda-gph*` tokens in pda.css + theme.css — everything else plain. ⚠ **CONTEXT LETTERS NOWHERE** on this reading now (kept on `PdaAnswers` for the city sheet + config lab), and **GREEN IS DOWN TO ONE BUNDLE**, the Skill's — both base conductors went amber because an agent and an interface are not encoded material. ⚠ **`CaseMapConfiguration.a` IS THE AGENT**, a single string; the fallback may NOT be a product (`MODEL_FAMILIES` bans `claude`; the LANE answers model class), so the non-tool vocabulary is run MODE — Chat assistant · Scheduled agent · Editor plugin · Coding agent — while tool CODENAMES stay in scope; the mapping is AUTHORED, with Vesper on `W-062` inferred and Babylon/Heimdall matching no stream), THICK multi-conductor bundles as the connectors (U3 — the mockup's thin traces lasted one pass). ⚠ **U4: THE CROP'S ASPECT IS THE CONTRACT** — `36 48 828 912` is PORTRAIT because the console's FIELD is portrait where this is read (0.876), and `meet` takes the minimum ratio, so the old LANDSCAPE crop was width-bound and left ~283px of dead panel no element move could reach; meet is 1.013 now and the smallest type paints 10.13px (was 5.55), at the named cost of horizontal letterboxing at short-wide viewports (hence the drawing's fs floor of **10**). **ONE SUB-CARD SIZE ACROSS ALL SIX** (side nodes stack their pair vertically, the wide base seats its pair in a row — 232×130, one line at fs 11.5). ⚠ **U6: the board is INSET 24 from the crop and the widths are ONE CHAIN** (`CHIP.x` IS `LEFT_X + NODE_W + GUTTER` — the side nodes had been flush on the crop's wall), and **the seat's dashed line takes the plate's GREEN at full weight** — at `--pda-dim`/0.75 the owner read it as absent, and a line quiet enough to be missed is not a subtle connection but a missing one (the DASH carries the grammar, the value need not). ⚠ **THE BINDING MEASURE IS A WORD, NOT A STRING** (`wrapLines` breaks on spaces; `RECONCILIATION` at 14 is the record's longest and every per-LINE assertion passes while it overflows — `pda-viewbox` walks words now and caught it immediately). **DELETED and guarded absent in unit + smoke:** the readout (U3, ADR-069's one-line contract overruled), the draw meter + NEVER A PRICE, the DRAWS ON caption, corner brackets, pad clusters, vias, crosses, and the arrowed dimension with its ticks. ⚠ **the rows STACK — the mockup's halves cannot hold the record** (96u wraps the 26-char worst onto three lines; full-width rows letter every value ON ONE LINE at fs 11, 194.5u of 196, walked for all 27 streams; owner at .08em not the mockup's .16; readout 11.5 = the outranking guard's ceiling). **The substrate bars are deleted** — the caption alone carries `DRAWS ON n OF m SHAPES` (both derived; `CONFIG_MAX_BARS` survives as the tap-ceiling pin), the crop stays `56 20 910 740` (4.97px = the CHROME's floor; values paint ~7.3px), and conductor-versus-content stays HAND-CHECKED on capture — every guard measures text, and both U1 collisions were invisible to all of them. The four losing archetypes stay at `/test/intelligence-config-lab` beside it. [ADR-069](sentinel/decisions/069-pda-selection-morph-and-answered-configuration.md) — **the selected work is the PERSISTENT OBJECT and it FLIES** between its two homes on any 1 ↔ 2 change (the core IS the clicked cartridge at `CORE_K`), which is what keeps the readings terminal display-switching rather than a zoom; the math is pure in `pdaFlight.ts` off ONE rect read, the dock class is deliberately NOT gated on `still`, and reading 02 prints the record's own answers (Skill/lane · `k[0]`/surface · context/graph · the bar) with one reactive readout — ⚠ `evals` and the joined `k` are arithmetically unletterable in a module, and a glyph state with no `fill` re-arms the person-led click bug. [ADR-063](sentinel/decisions/063-map-reading-rail-and-wheel.md) — the reading rail is HORIZONTAL across the top, and the console OWNS THE WHEEL while the pointer is on it, **releasing at both ends** (the beat is scroll-pinned; a console that kept it would trap the page). ⚠ [ADR-062](sentinel/decisions/062-intelligence-map-city.md) is **stale on the drawing** — its isometric city (`map/MapSurface.tsx`) is still on disk and still tested but is NOT what the landing renders; its placement, evidence semantics and confidentiality envelope still bind. ⚠ author at **1280×720**. The console has NO head and NO foot title (U1: everything removed was height, and height is the only currency the drawing spends); every type size is derived from a measured box, each reading crops its own viewBox, and **label-on-label overlap is the check nothing else does**. Readings 01/03 still letter at ~4.5–5.5px against an 8.5px floor and no tuning lever remains — the gap is DENSITY (ADR-063 §Outstanding, owner call)
- **Rail manifest + the journey indicator:** [ADR-031](sentinel/decisions/031-rail-manifest.md) (the rail, the tick ladder, the detent data) → [ADR-055](sentinel/decisions/055-corner-section-readout.md) — the section readout lives in the TOP-RIGHT NAV CORNER and doubles as the drawer trigger; the left/right section menus are deleted and there are no subsections anywhere
- **Rail instruments + the four-corner scheme:** [ADR-059](sentinel/decisions/059-rail-instruments.md) — the journey (Home · Thesis · Arc · Proof · Services · About · Voidwalker) TOP-LEFT, nav TOP-RIGHT, brand BOTTOM-LEFT, and Contact · session · theme switch on ONE LINE BOTTOM-RIGHT (U3 — the switch is the outboard ANCHOR; new icons go to its LEFT); plus the right rail's bearing/sector/local telemetry (the vertical section name is REMOVED — the nav corner already names it). Flags `RAIL_INSTRUMENTS` / `SETTINGS_CLUSTER`. ⚠ two clocks that throw nothing when confused — the Arc must stay a BEAT RANGE or it double-lights with Thesis (`tests/lib/rail-instrument-marks.test.ts` is the guard); `#practice` has no mark pending its removal; the bottom-right row's curtain clip is a COPY of `.hud__corner--br`'s. ⚠ **U5 (2026-08-14): the session mark IS the session** — it is a `button` opening a panel with the name · `ACTIVE` · `Admin tools` · `Log out`, and the global `components/auth/UserStatus` overlay that used to print that in the NAV's corner at z 1000 is DELETED (`Providers.tsx` mounts no identity readout). The glyph stays bare at rest (U2 §2's measurement, not a preference), the panel copies `.hud__nav__list`'s grammar because the frame has exactly two press-to-open panels, and its right edge is the control GROUP's — 17px outboard of the track, which is U4's arithmetic one level up. Sign-out away from the landing is `/admin`'s own `SessionActiveShell`. Look-dev at `/test/hud-instruments-lab`
- **Client arcs (/arcs):** [ADR-052](sentinel/decisions/052-client-arcs.md) — client deck pages (workshop/keynote) on the HUD slice; content model in `lib/arcs/`, rules in [`.claude/rules/arcs.md`](.claude/rules/arcs.md). "Arc page" ≠ "the Arc" (LANGUAGE.md). → [ADR-057](sentinel/decisions/057-arc-terminal-motion.md) — **terminal motion**, the pinned-beat grammar on the `-v2` cuts (masthead decodes in place, panels power on, the plane folds LIFO behind an iris); selected per arc by `ArcDef.motion`, so the v1 pages stay byte-identical. → [ADR-072](sentinel/decisions/072-portfolio-arc-and-dossier.md) — **the portfolio arc** (`/arcs/portfolio`, for Rob: hero → about → ONE overview from adoption to automation → five shapes → the roster → four tool **dossiers** → what changed → close) and the ninth section kind, `dossier` = `{ toolId, legend, head? }`, one tool per beat, drawn from `PROJECT_CASES` and mounting the casefile's own console (`ToolField`, extracted from `ToolGallery` behind a snapshot pin) at page scale. ⚠ The Loop evidence is SHARED BY REFERENCE (`lib/arcs/content/shared/*`); the numbers canon fails on every arc and the confidentiality envelope on the portfolio (the keynote deck keeps its € rows — recorded); a dossier beat must FIT at the three reference shapes; the arcs' light theme is partial and pre-existing (left open). → [ADR-073](sentinel/decisions/073-arc-header.md) — **the site's header on every arc** (`ArcHudNav`: chapter links in the hero, the section readout + the full drawer once scrolled, the landing's own `.hud__nav*` chrome) and **the left reel is DELETED** — it only rendered above 1101×760, so 1280×720 had no navigation at all (ADR-055's ruling, one surface later); `menuPrimary` marks a chapter (≤5, registry-pinned), and the arc hero grew a top scrim because the key visual is near-white where the header sits (1.06:1 → 6.1–7.2:1 measured). → [ADR-075](sentinel/decisions/075-arc-hero-curtain.md) — **the arc hero IS the homepage hero**: `hero.plate: "gateway"` takes the landing's plate + glitch (and fixes the accident where EVERY arc showed the Gateway in light), `useHeroBoot` is the landing's boot extracted and shared, and the CURTAIN SEAM is ported from ADR-022 v8 — the card is the MOVER and the first beat's `.arc-plane` is held fixed against `data-arc-entry` (freeze the plane, never the stage, or every terminal clock desyncs). → [ADR-076](sentinel/decisions/076-portfolio-flows-and-the-architecture-beat.md) — **the portfolio FLOWS and the architecture closes it**: `motion` comes off the portfolio (a deck is presented, a portfolio is scrolled — 25.7 → 12.6 viewports), the ADR-075 curtain is generalised to the reveal path (it freezes the first section's own `.arc-band`; `useArcScroll` writes `data-arc-tall` itself), and the two Skills text walls (5,319px of roster + five ruled rows) become ONE `intelligence` section at the foot mounting the casefile's three-reading map console at page scale on the shared `LOOP_INTELLIGENCE_MAP`. ⚠ The box's ASPECT is the contract — a panel wider than the crop letterboxes horizontally and a height-only fill guard reports green (ADR-070's finding, a fourth time); ⚠ never declare `data-proof-settled` on that host or the console's wheel gate arms a scroll trap mid-page. → [ADR-078](sentinel/decisions/078-portfolio-proof-page.md) — **the portfolio IS the casefile, expanded into a page**. ⚠ **U1 (2026-08-24, owner) IS THE LIVE CUT AND IT REJECTED U0's OPENING**: the hero/bio/origin-cards/thesis/bridge (FOUR sections of throat-clearing on an extension of the proof panel) collapse into ONE `program` beat, sixteen ids → eleven. ⚠ **THE `flywheel` DREW A METAPHOR AND IS DELETED** — a ratchet of strands and teeth that had to be explained before it meant anything, in a house where every instrument draws a RECORD (the dossiers draw real tool UIs, the map 47 real Skills, the sheets real ads). **`kind: "program"`** plots the engagement on a DATED axis instead — graticule, adoption curve as a step ladder, what shipped at its real date as an anchor into its own chapter, framed registers, the seat where both arrive; ⚠ **THE GAPS ARE THE READING** (`at` authored from the record, registry-pinned sorted — an even spread deletes the one thing the chart knows that a list does not), it letters NO figures and no digits but YEARS, and it must fit ONE VIEWPORT at 1280×720 or `data-arc-tall` disarms the curtain. ⚠ **THE COPY LAW**: a title is a NAME, not an aphorism — the counting pair, the reversal epigram and the spelled-out-number opener are banned as DISPLAY titles and walked over every `head.title` (a dated LOG ROW may still say them; a record is not a claim). ⚠ **THE CURTAIN WAS GATED ON THE PLATE** and is not any more — a hero declares `curtain: true`, because coupling a choreography to an image meant a Loop key visual would have silently killed the ADR-076 seam; `HERO_ROUTES` is hand-written so its portfolio row came out by hand, and the own-plate top scrim is a dark LITERAL (theme.css re-pins `--void-deep-rgb` on any `.hero__video__overlay`, washing parchment over a photo in light). The U0 record follows: three new section kinds (`sheets` · `films` · `flywheel`), the page re-cut in narrative order (about → beyond → the flywheel → studio → reel → tools → four dossiers → rollout → architecture → close, with `interstitial` bridges carrying the case shape's _what it revealed next_), and the studio chapter mounting the casefile's own `SheetsPlate`/`FilmsPlate` on `LOOP_STUDIO_SHEETS`/`LOOP_ATL_FILMS` (exported, pinned `toBe`). ⚠ **The flywheel is a RATCHET, not a wheel** — no circle on it: a green people-rail, a staircase that only rises, teeth falling to each riser, dashed lifts landing one tread AHEAD; it letters NO digits (registers read `LOOP_FIGURES` in the renderer) and its route strip deep-links every chapter. ⚠ **DON'T COPY THE CONSOLE BEATS' ASPECT CAP ONTO A DOM DRAWING** — `.arc-intel`/`.arc-films` cap width against height because SVG `meet` letterboxes; nothing on the flywheel is scaled, and the cap discarded 340px of band at 1440×800. ⚠ **A composition claim stated only in a comment is not true yet**: the route ran full width and put the terminus 325px from the exit column, under the register stack, reading as a caption beside the numbers — it sits in the field's own column now, and the guard pins the seat under the drop. ⚠ `SheetsPlate` gained `stillSizes` (a `sizes` hint is a statement about the BOX) — every OTHER edit to either plate is a two-surface change → [ADR-077](sentinel/decisions/077-arcs-ink-ramp.md) — **the arcs get an ink ramp**: every colour on the surface was a RAW LITERAL (`rgba(235,227,214,α)` = cream-on-black spelled out), which is exactly what ADR-058's `--dawn-rgb`/`--void-rgb` swap cannot reach — so the light theme painted near-black cards on parchment. `--arc-ink-*`/`--arc-edge`/`--arc-plate` on `.arc-root`, re-derived (never inherited) in light because an alpha inverts its own meaning across the flip; dark byte-identical bar one deliberate lift (10px meta labels were 3.19:1). ⚠ The photo scrims stay literal (kept-dark imagery), and the smoke's parity walk COMPOSITES before measuring
- **Workshop corridor variant (/claude-workshop):** [ADR-053](sentinel/decisions/053-workshop-corridor-variant.md) — the homepage variant with **About second** (hero → about → corridor → services → contact); parse options + prototype surgery + `.cw-root`-scoped CSS. ⚠ the about-stage portal slot must never exist in that prototype (it kills the services ring)
- **Hero key visual, per theme:** [ADR-058 Update 2](sentinel/decisions/058-light-mode-theme.md) — the hero is NO LONGER a dark artifact in light mode (§5 reversed): dark `Gateway_v1b.avif` in a `<picture>`, light `Gateway_v2-light.webp` as a CSS background, and the theme-picked preload is script-injected in `app/layout.tsx` (`lib/theme/heroPreload.ts`) because the preload scanner beats every script. ⚠ neither theme may fetch the other's plate — `loading="lazy"` + `display:none` is what enforces it, and you must measure it in a FRESH tab. → [ADR-060](sentinel/decisions/060-hero-theme-glitch.md) — the swap plays a **slice-tear + pixel-resolve glitch**: a canvas over an already-flipped hero, painted synchronously inside the store notify so there is no flash. Kernel `lib/key-visual/themeGlitch.ts` (the `done` frame MUST be the identity or the canvas removal pops); plates warm on toggle HOVER, never on idle
- **Corridor quality governor:** [ADR-038](sentinel/decisions/038-corridor-quality-governor.md) — GPU probe + FPS degradation
- **Landing v7 compositing:** [ADR-008](sentinel/decisions/008-landing-v7-background-layers.md)
- **Brandmark choreography / particles:** [ADR-010](sentinel/decisions/010-brandmark-choreography.md), [ADR-011](sentinel/decisions/011-brandmark-particle-artifact.md), [ADR-013](sentinel/decisions/013-brandmark-journey-refactor.md)
- **Scroll architecture:** [ADR-002](sentinel/decisions/002-scroll-animation-architecture.md)
- **Auth centralization / Supabase RLS:** [ADR-003](sentinel/decisions/003-auth-centralization.md), [ADR-037](sentinel/decisions/037-supabase-rls-trust-boundary.md)
- **Landing data caching:** [ADR-028](sentinel/decisions/028-landing-data-caching.md)
- **Focus overlay system:** [ADR-006](sentinel/decisions/006-focus-overlay-system.md)

When path-scoped rules in [`.claude/rules/`](.claude/rules/) match your files, follow them. Don’t import the whole `sentinel/` tree into prompts — use ADRs, rules, and skills on demand to save context.

## Working on this codebase

- **Before non-trivial work:** open [sentinel/MAINTENANCE.md](sentinel/MAINTENANCE.md) — use **Cycle A** for bugfix follow-up, **Cycle B** for new feature surface.
- **At the end of a session that changed code:** run the **post-incident checklist** in [sentinel/MAINTENANCE.md](sentinel/MAINTENANCE.md); if a row triggers, update ADR / BEST-PRACTICES / rule / skill before pushing.
- **Vocabulary:** [LANGUAGE.md](LANGUAGE.md) — use terms like _Module, Seam, Station, Actor_ consistently.

**Hooks & CI:** [lint-staged](package.json) runs ESLint + Prettier on staged `*.{ts,tsx}` (and Prettier on `*.{json,css,md}`). CI is [`.github/workflows/verify.yml`](.github/workflows/verify.yml): `npm run verify` (lint + typecheck + unit tests) and a production build, plus Playwright corridor smokes and react-doctor (new-issues-only on PRs). Optional: [`.cursor/hooks.json`](.cursor/hooks.json) + [`scripts/sentinel-pre-edit-hint.mjs`](scripts/sentinel-pre-edit-hint.mjs) inject a one-line Sentinel hint on matched writes.

---

This is the Thoughtform.co website, a Next.js application with a sophisticated particle system and HUD-based navigation.

## Tech Stack

- **Framework**: Next.js 16 (App Router; `proxy.ts` is the Next 16 rename of `middleware.ts`, and route params are Promises)
- **UI**: React 19, Tailwind CSS
- **State**: Zustand
- **Animation**: Framer Motion, GSAP
- **3D**: Three.js, React Three Fiber, Drei
- **Database**: Supabase

## Project Structure

```
app/
  (marketing)/          # Public landing page (V7 prototype composite)
  (admin)/              # Gated tools: /admin, /astrogation, /orrery
  (internal)/           # Dev-only routes: /test/* (proxy-blocked in prod)
  api/                  # API routes (celestial, particles, survey, etc.)
components/
  landing/v7/           # V7 landing: LandingPage, CelestialConnector
  landing/home-v2/      # Depth corridor (DepthGatewayScene, services, arc-cases)
  hud/                  # Navigation chrome (NavigationBar, frames, status)
  gateway/              # ImageParticleGateway, ThreeGateway, KeyVisualPortal
  particles/            # ParticleCanvasV2, ThoughtformSigil, WordmarkMorph
  admin/                # Admin overlays (CelestialEditor, AdminGate)
  auth/                 # AuthProvider
  ui/                   # Shared UI primitives
lib/                    # Utilities, hooks, domain modules (celestial/, auth/, etc.)
supabase/               # Database schema and migrations
docs/                   # All non-decision documentation
  design/               # Mockups, brand explorations, inspiration
  plans/                # Work-in-progress plans
  issues/               # Issue tracker notes (was .linear-issues/)
sentinel/               # Decision records (ADRs), BEST-PRACTICES, MAINTENANCE
```

## Key Components

### Navigation System

- `NavigationBar.tsx` - Top navigation with mobile section indicator
- (The old `NavigationCockpitV2` scroll HUD and its `/archive/current-home`
  route were deleted in the 2026-07 Phase 5 cleanup — git history is the
  archive.)

### Particle System

- `components/particles/ParticleCanvasV2.tsx` - Main particle canvas
- Config stored in Supabase `particle_config` table
- Admin panel at `/admin` for live editing

### Landing Page (V7)

- `app/(marketing)/page.tsx` - Server component that fetches V7 content and celestial slot data
- `lib/v7-parse/` - Reads and parses `public/prototypes/v7/landing-v7-motion.html` at build time (entry `lib/v7-parse/index.ts`; body/CSS/section/rail parsers alongside)
- `components/landing/v7/LandingPage.tsx` - Client component: scroll, reveal, sigil choreography
- `components/landing/v7/CelestialConnector/` - Parametric celestial diagrams between sections

## Database

- Uses Supabase with Row Level Security (RLS)
- Key tables: `particle_config`, `service_sigils`, `celestial_designs`, `celestial_slots`, `survey_items`
- Schema in `supabase/schema.sql`, RLS policies in `supabase/auth-rls.sql`
- Migrations in `supabase/migrations/` (timestamp-prefixed)

## Development Commands

```bash
npm run dev      # Start development server (localhost:3003)
npm run build    # Production build
npm run lint     # Run ESLint
npm run format   # Format with Prettier
```

### Naming a page — give the FULL URL, never the path

Per the global rule, any reply that points at a page on this site prints the
whole thing, on its own line: `http://localhost:3003/test/voidwalker-holo-gallery`.
A route group is invisible in the URL, so the mapping is mechanical:

| file                                   | URL                                 |
| -------------------------------------- | ----------------------------------- |
| `app/(internal)/test/<name>/page.tsx`  | `http://localhost:3003/test/<name>` |
| `app/(marketing)/page.tsx`             | `http://localhost:3003/`            |
| `app/(marketing)/arcs/[slug]/page.tsx` | `http://localhost:3003/arcs/<slug>` |
| `app/(admin)/<tool>/page.tsx`          | `http://localhost:3003/<tool>`      |

⚠ **3003 is the default, not a guarantee** — `scripts/dev-server.mjs` honours a
harness-assigned `PORT` and `.claude/launch.json` sets `autoPort`, precisely so
a second session in this tree gets a free port. Read the port off the running
server (`preview_start` / `preview_list`, or curl the candidate) before quoting
a URL; the other session's server is usually the one to attach to.
⚠ `(internal)` routes are proxy-blocked in production, so a `/test/*` page has
**no** thoughtform.co URL to offer alongside the local one.

## Skills Available

Skills live in [`.claude/skills/`](.claude/skills/) and load on demand — don't pull them all into context. Current set:

- **context7** — fetch up-to-date, version-specific library docs. Trigger: "use context7", "get docs for [library]".
- **frontend-design** — front-end / component / responsive design decisions that hold the HUD aesthetic and avoid generic patterns.
- **landing-v7-compositing** — stacking / gateway / hero / shield rules for `components/landing/v7/**` (pairs with ADR-008).
- **landing-performance** — load-order, code-split, and payload doctrine for the landing route (keeps the WebGL corridor and Supabase off First Load JS).
- **brandmark-choreography** — the scroll-driven brandmark journey (continuous transform model, ADR-013).
- **brandmark-particle** — the brandmark particle painters (atmosphere / silhouette / substrate-sphere).
- **thoughtform-design** — Thoughtform-specific design overrides (tokens, shape law, HUD grammar).
- **theme-parity** — both-themes discipline (ADR-058): every new element ships styled AND verified in dark + light; DOM alpha lifts, WebGL palette pairs, per-theme assets, the verify recipe.

## Conventions

- Use TypeScript strictly
- Follow existing code patterns
- Mobile-first responsive design
- CSS variables for theming (--gold, --dawn, etc.)
- Scroll-driven animations with progress values 0-1

### Imports and barrels

- **Feature barrels** (`components/hud/index.ts`, `components/gateway/index.ts`, `lib/celestial/index.ts`) are kept current and are the preferred import path for that feature's public API.
- **Root barrel** (`components/index.ts`) is a historical compatibility layer recording the ADR-004 archival (the `legacy/` tree itself was deleted 2026-07-23 — git history is the archive). Do not add new exports there; import from feature barrels directly.
- Prefer `@/components/feature` or `@/lib/module` over deep path imports when a barrel exists.

### Supabase migrations

- New migration files must use the `YYYYMMDD_descriptive_name.sql` naming convention (e.g. `20260421_celestial_connectors.sql`).
- Older files without date prefixes (`add_presets_to_particle_config.sql`, etc.) are legacy; do not rename them as they are already in the remote migration history.

### Route groups

- `(marketing)` — public-facing pages (the landing page).
- `(admin)` — tools gated by `AdminGate` + `useAuth` (admin login, astrogation, orrery).
- `(internal)` — dev-only routes (`/test/*`) blocked by `proxy.ts` in production (the Next 16 rename of `middleware.ts`).
- New public pages go in `(marketing)`. New admin tools go in `(admin)` with `AdminGate`. New dev/test pages go in `(internal)`.

## Design System Patterns

### Focus Overlay System (ADR-006)

When building any modal, detail view, or focus overlay, use these patterns:

**Required CSS Variables** (defined in `.astrogation`):

```css
--focus-overlay-bg: rgba(10, 9, 8, 0.2);
--focus-overlay-blur: 12px;
--focus-overlay-border: rgba(235, 227, 214, 0.3);
--focus-backdrop-bg: rgba(10, 9, 8, 0.3);
```

**Required Animation** - Apply to the CONTENT element, not the backdrop:

- `assetFocusIn` - for absolutely positioned content (`top: 50%; left: 50%`)
- `modalFocusIn` - for flexbox-centered content (`display: flex; align-items: center`)

```css
animation: assetFocusIn 0.3s ease-out; /* or modalFocusIn */
```

**Required Box Shadow**:

```css
box-shadow:
  0 0 0 1px rgba(235, 227, 214, 0.05),
  0 0 60px rgba(202, 165, 84, 0.1),
  0 30px 80px rgba(0, 0, 0, 0.6);
```

**Required Structure**:

1. Fixed container with backdrop (uses `--focus-backdrop-bg`)
2. Centered overlay with animation (`assetFocusIn`)
3. Content frame with dashed border (`--focus-overlay-border`)
4. Label badge on top (if content has a title)

**Size variants** (small / medium / large): see `sentinel/decisions/006-focus-overlay-system.md`.

### Panel Layout

Both side panels use identical dimensions:

- Width: `340px`
- Margin-top: `40px`
- Height: `calc(100vh - var(--hud-padding, 32px) * 2 - 120px)`

### Grid Item Hover States

All clickable grid items should use:

- `transform: translateY(-2px)` on hover
- `border-color: var(--gold-30)` on hover
- `box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4)` on hover

See `sentinel/decisions/006-focus-overlay-system.md` for full documentation.

### Landing v7 Compositing (ADR-008)

Stacking / gateway / hero / shield rules, checklists, and full regression history: [ADR-008](sentinel/decisions/008-landing-v7-background-layers.md) and `.claude/skills/landing-v7-compositing/SKILL.md` (and brandmark ADR-010 for the fixed mark).
